# Upstash Redis REST credentials.
# Get these from your Upstash project: create a Redis database in the Upstash
# console, then copy the REST URL and REST TOKEN from that database's details page.
UPSTASH_REDIS_REST_URL="https://example.upstash.io"
UPSTASH_REDIS_REST_TOKEN="example-token"

# Optional IP geolocation API key used by app/api/geo/route.ts.
# Get this from ipgeolocation.io: create an account, then copy an API key from
# the dashboard / API Keys page.
GEO_IP_API_KEY=""

# Optional editor password. When set, /editor and its write APIs require
# unlocking once in the browser before files can be changed.
# This value is self-managed: generate your own strong random string and keep it
# in local or production secrets instead of committing it to the repository.
EDITOR_ACCESS_TOKEN=""

# Optional override for the editor session cookie "Secure" flag.
# Leave empty for auto mode:
# - HTTPS requests -> secure cookie enabled
# - HTTP requests  -> secure cookie disabled
# Set to "1"/"true" to force secure cookies, or "0"/"false" to disable.
EDITOR_ACCESS_COOKIE_SECURE=""
